import styled from "styled-components";

export const FooterContainer = styled.footer`
  display: flex;
  justify-content: center;
  align-items: center;
  height: 76px;
  background-color: var(--background);
`

export const FooterText = styled.address`
  font-size: 1.4rem;
  font-style: normal;
  font-weight: 400;
  line-height: 1.4;
  letter-spacing: 0em;
  text-align: center;
  color: var(--dark-green);`
